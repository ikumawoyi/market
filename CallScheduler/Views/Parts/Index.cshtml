@model PaginatedList<Part>

@{
    ViewData["Title"] = "Parts";
    var index = (int)((Model.PageIndex - 1) * 10) + 1;
    // var firstPage = 1;
    var lastPage = ViewBag.NumberOfPages;
}

<div class="colorlib-blog" style = "font-size:25px">
    <div class="container">
        <div class="row">
            <div class="col-md-12 text-center colorlib-heading animate-box">
                <h2>
                    <span style="float:left;">Parts</span>
                    <a href="/Parts/Create" class="btn btn-primary btn-sm" style="float:right;"> Add Part</a>
                </h2>
                @if (Model.Count == 0)
                {
                    <p>
                        No Part available.
                    </p>
                }
            </div>
        </div>
        @if (Model.Count != 0)
        {
            <div class="row">
                <div class="col-lg-12">
                    <div class="hpanel">
                        <div class="panel-heading">
                            <div class="panel-tools">
                                <a class="showhide"><i class="fa fa-chevron-up"></i></a>
                                <a class="closebox"><i class="fa fa-times"></i></a>
                            </div>
                        </div>
                        <div class="panel-body no-border no-shadow">
                            <div class="table-responsive">
                                <table cellpadding="1" cellspacing="1" class="table table-bordered table-striped">
                                    <thead class=" text-primary">
                                        <tr>
                                            <th>S/N</th>
                                            <th>Code</th>
                                            <th>Name</th>
                                            <th>Variant</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var mess in Model)
                                        {
                                        <tr>
                                            <td>@(index++)</td>
                                            <td>@mess.PartCode</td>
                                            <td>@mess.Name</td>
                                            <td>@mess.MachineVariant.Name</td>
                                            <td>
                                                <a class="badge badge-primary" asp-area="" asp-controller="Parts" asp-action="Edit"
                                                   asp-route-id="@mess.Id">Edit</a>
                                                <a class="badge badge-primary" asp-area="" asp-controller="Parts" asp-action="Delete"
                                                   asp-route-id="@mess.Id">Delete</a>
                                            </td>
                                        </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <div class="panel-footer">
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

